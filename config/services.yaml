parameters:
    api_token: '%env(API_TOKEN)%'

services:
    _defaults:
        autowire: true
        autoconfigure: true
        bind:
            $uploadsDir: '%kernel.project_dir%/public/images/uploads/'
            $uploadsPublicDir: 'images/uploads/'

    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'
            - '../src/Tests/'

    App\Controller\:
        resource: '../src/Controller/'
        tags: ['controller.service_arguments']
