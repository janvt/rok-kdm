{% if is_granted('ROLE_SCRIBE') %}
    <a href="{{ path('scribe_index') }}">Scribe Corner</a> |
{% endif %}
{% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('app_admin_dashboard_index') }}">Admin</a> |
{% endif %}
<a href="{{ path('app_logout') }}">logout</a>
