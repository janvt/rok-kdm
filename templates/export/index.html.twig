{% extends 'base.html.twig' %}

{% block page_title %}{% endblock %}

{% block body %}
    <div class="card mb-4 shadow-sm">
        <div class="card-header pb-0">
            <h3>Exports</h3>
        </div>
        <div class="card-body">
            <a class="btn btn-primary" href="{{ path('export_gov_data') }}">Export Governor Data</a>
            <a class="btn btn-primary ml-2" href="{{ path('export_commander_data') }}">Export Commanders</a>
        </div>
    </div>
{% endblock %}