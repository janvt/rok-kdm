{% extends 'base.html.twig' %}

{% block title %}Governor{% endblock %}

{% block body %}
    <h1>Governor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ governor.id }}</td>
            </tr>
            <tr>
                <th>Governor_id</th>
                <td>{{ governor.governorId }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ governor.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('governor_index') }}">back to list</a>

    <a href="{{ path('governor_edit', {'id': governor.id}) }}">edit</a>

    {{ include('governor/_delete_form.html.twig') }}
{% endblock %}
