<div class="card officer-actions mb-3  shadow-sm">
    <div class="card-header">
        <h5>Officer Actions</h5>
    </div>
    <div class="card-body">
        <a class="btn btn-primary" href="{{ path('governor_add_note', {'id': gov.id}) }}">Add Note</a>
        <a class="btn btn-primary ml-2" href="{{ path('governor_edit', {'id': gov.id}) }}">Edit</a>
    </div>
</div>