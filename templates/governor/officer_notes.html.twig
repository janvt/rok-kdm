{% if gov.officerNotes %}
<div class="card mb-3 shadow-sm">
    <div class="card-header">
        <h4>Officer Notes</h4>
    </div>
    <div class="card-body">
        <div class="officer-notes">
            {% for officerNote in gov.officerNotes %}
                {% include 'governor/officer_note.html.twig' with {officerNote: officerNote} only %}
            {% endfor %}
        </div>
    </div>
</div>
{% endif %}